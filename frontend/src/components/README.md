# Componentes compartidos

Este directorio contiene solo UI o layout reutilizable por varias features. Un componente que
pertenece a una capacidad de negocio vive en `src/features/<feature>/components`.

No se crean componentes de las HUs hasta que su feature entre en desarrollo.
