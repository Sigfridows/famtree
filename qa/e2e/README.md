# Pruebas E2E

Playwright está configurado desde `frontend/playwright.config.ts`. Los specs se agregarán aquí cuando existan flujos verticales implementados; el bootstrap no inventa flujos de negocio.
